# API Version 與 Resource Type
apiVersion: v1
kind: Secret
metadata:
  name: app-secret         # Secret 名稱，供 Deployment 中的 secretKeyRef 引用
type: Opaque               # 預設類型，用於儲存 Arbitrary Data (任意格式數據)
data:
  # 注意：所有 Value 必須經過 Base64 Encoding
  # 請自行產生編碼後填入，例如使用指令：echo -n 'your_password' | base64
  db-pass: <PLACEHOLDER_FOR_BASE64_DATABASE_PASSWORD>
  rmq-pass: <PLACEHOLDER_FOR_BASE64_RMQ_PASSWORD>
